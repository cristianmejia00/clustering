# Line endings: shell scripts and Dockerfile must stay LF even on Windows
# checkouts / ZIP downloads; .bat files must be CRLF to run reliably.
* text=auto
*.sh text eol=lf
Dockerfile text eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf
